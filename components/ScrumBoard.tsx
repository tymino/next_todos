import React, { useState, useEffect } from 'react';

// import dynamic from 'next/dynamic';
// const ItemContainer = dynamic(import('./ItemContainer'));

// interface IScrumBoardProps {
//   items: any;
// }

// const ScrumBoard: React.FC<IScrumBoardProps> = ({ items }) => {
//   const [winReady, setwinReady] = useState<boolean>(false);
//   useEffect(() => setwinReady(true), []);

//   return <>{winReady ? <ItemContainer items={items} /> : null}</>;
// };
// export default ScrumBoard;
